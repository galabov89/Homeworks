function replaceSpaces(text) {
    text = text.replace(/\s+/g, '&nbs');
    console.log(text);
}

var text = "But you were living in another world tryin' to get your message through'";

replaceSpaces(text);